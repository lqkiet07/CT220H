import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFE50914); // Tông đỏ phong cách điện ảnh
  static const Color background = Color(0xFF121212); // Nền tối
  static const Color surface = Color(0xFF1E1E1E); // Màu nền của các thẻ (Card)
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;

  // Màu cho sơ đồ ghế
  static const Color seatAvailable = Color(0xFF2C2C2C);
  static const Color seatSelected = primary;
  static const Color seatBooked = Color(0xFF555555);
  static const Color seatVip = Color(0xFFFFD700); // Vàng cho ghế VIP
  static const Color seatSweetbox = Color(0xFFFF69B4); // Hồng cho Sweetbox
}
